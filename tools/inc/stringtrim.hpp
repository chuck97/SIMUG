#pragma once
#include <string>
#include <algorithm>

namespace SIMUG
{
    namespace tools
    {
        void ltrim(std::string& s);
        void rtrim(std::string &s); 
        void trim(std::string &s);
    }
}